[package]
name = "wowterminal"
version = "0.13.5"
description = "AI Terminal with multi-backend AI (cloud/local/self-hosted) and SSH manager"
authors = ["cuter74-dev"]
license = "MIT"
edition = "2021"

[lib]
name = "wowterminal_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
anyhow = "1"
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "json", "stream"] }
url = "2"
futures-util = "0.3"
uuid = { version = "1", features = ["v4", "serde"] }
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
aes-gcm = "0.10"
argon2 = "0.5"
rand = "0.8"
zeroize = { version = "1", features = ["zeroize_derive"] }
base64 = "0.22"
toml = "0.8"
portable-pty = "0.9"
russh = "0.61"
dirs = "5"
tempfile = "3"
russh-sftp = "2"
ssh-key = { version = "=0.7.0-rc.10", features = ["ed25519", "rsa", "encryption"] }
tauri-plugin-dialog = "2.7.1"
tauri-plugin-updater = "2.10.1"
tauri-plugin-process = "2.3.1"
machine-uid = "0.6.0"
tauri-plugin-notification = "2.3.3"
# 에러·크래시 추적(GlitchTip = Sentry 호환). default-features 끄고 rustls 사용
# (프로젝트 전역이 rustls라 native-tls/openssl 의존을 피한다).
sentry = { version = "0.34", default-features = false, features = ["backtrace", "contexts", "panic", "reqwest", "rustls"] }

[dev-dependencies]
wiremock = "0.6"
tokio = { version = "1", features = ["full", "macros", "test-util"] }

