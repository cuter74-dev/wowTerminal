# 2026-08-12

## #148 — 기본 다운로드 폴더 설정

**무엇**: 다운로드할 때마다 폴더 선택 다이얼로그가 떠서 매번 대상 폴더를 골라야 했다. 설정에 "기본 다운로드 폴더"를 추가하고, 다운로드는 묻지 않고 그 폴더로 바로 저장. 특별히 다른 곳에 받고 싶을 때만 새 컨텍스트 메뉴 "폴더 지정 다운로드…"로 고른다.

**어떻게**:
- `settings.ts`: `general.downloadDir` 추가(기본 `""` = OS 다운로드 폴더).
- `SettingsModal.tsx`: 일반 탭에 입력란 + 폴더 선택 버튼(로그 폴더 패턴 재사용). 11개 언어 라벨.
- `FileBrowser.tsx`:
  - `resolveDownloadDir()`: 설정값 우선, 비우면 `@tauri-apps/api/path`의 `downloadDir()`. `~`/`~/…`는 `homeDir()`로 확장(Rust `sftp_download`는 경로를 그대로 쓰므로 프론트에서 해소).
  - `doDownload(entry, askDir)`: 기본은 다이얼로그 없이 **현재 로컬 패널 폴더**로 저장(사용자 피드백 — 패널이 기본 다운로드 폴더에서 시작하므로 안 움직이면 기본 폴더, 이동했으면 그 위치; 이름 충돌 해소 #137은 대상 폴더 기준으로 그대로 동작). `askDir=true`(컨텍스트 메뉴 "폴더 지정 다운로드…")면 종전처럼 폴더 선택.
  - 폴백: OS 다운로드 폴더를 못 얻는 환경(모바일 등)은 로컬 패널 cwd → 그것도 없으면 폴더 선택.
  - 패널 간 드래그 다운로드는 종전대로 보이는 로컬 패널 cwd에 저장(드롭 대상이 명시적이므로).
  - 로컬 패널 시작 위치도 기본 다운로드 폴더(사용자 추가 요청). 경로 오류 시 홈으로 폴백.

## v0.20.13 릴리스 (#147, #148)

- **데스크탑**: v0.20.13 태그 → CI 성공 → 에셋 17종·latest.json(11개 플랫폼 전부 서명) 검증 → publish(Latest). 기존 사용자 자동 업데이트.
- **Android**: 클린 사본(/private/tmp/wt2013)에서 release aab(20013, 업로드 키 서명 99:0E:8B:59…, 4 ABI) → Play production completed commit. release 서명 태블릿도 Play 업데이트 수신.
- **GitLab**: 한국어 릴리스 커밋 4d8509e 푸시.
- 사용자 보고 "글자 쓰다 스페이스 누르면 글자 사라짐"(배포판 0.20.12)은 #147 수정 미포함 상태였음 — 0.20.13에 포함되어 해소 예상. 재발 시 재추적.
- iOS는 1.0.9 심사 진행 중이라 이번에도 제외 — 통과 후 다음 사이클에 포함.

## iOS 1.0.10 심사 제출

- 1.0.9 심사 통과 확인(사용자) → 다음 사이클 진행.
- ios-testflight.yml dispatch(build_number 20013) → CI 성공, 빌드 `0.20.13.20013` VALID.
- ASC API: appStoreVersion **1.0.10**(AFTER_APPROVAL) 생성 → 빌드 연결 → whatsNew(en-US) → `usesNonExemptEncryption:false` → reviewSubmission → **WAITING_FOR_REVIEW**.
- 포함: #144(AI 헤더·크래시 화면), #145(API 키 토글), #147(한글 혼용), #148(기본 다운로드 폴더).
