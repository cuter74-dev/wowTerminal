# 2026-06-11 Work Log

## #83 follow-up — M5 stray chars persist past writingsuggestions; rewrite-storm suppression (v0.14.2)

GitHub #83 (English) / GitLab #2 (Korean).

- The M5 user reports the `ddf` bug persists on builds with `writingsuggestions="false"` (v0.13.12+). So the attribute doesn't stop whatever routes English typing through keyCode 229 + textarea rewrites on that machine (third-party IME? newer-macOS behavior the attribute doesn't govern?).
- New approach — fix the damage instead of the trigger: the visible corruption always comes from the mirror's multi-backspace rewrite storms (v0.13.5 trace: 15 chars + 10 backspaces for 5 keys). `flushMirror` now refuses rewrites that erase 2+ chars of **pure-ASCII** content: a terminal wants raw keystrokes, so OS-driven rewrites are ignored (tracking resyncs; subsequent appends flow normally). Appends, single backspaces (real Backspace key), and any value containing CJK (genuine composition rewinds) behave exactly as before, so old-WKWebView Korean is untouched.
- Re-added the lightweight macOS-only counters diag (`wt-ime-diag2`: n229/nCS/onDataChars/mirrorBs/mirrorChars/**bsSuppressed** + the writingsuggestions attribute value), sent on 3s input idle, ≤4 sends/session — verifies on the M5 whether 229 still fires and whether suppression is doing the work.

## Release v0.14.2
- Patch. 0.14.1 → 0.14.2 in the 4 version files. Tag `v0.14.2` → CI → auto-publish. M5 user types `df -h` to confirm; the diag tells us the path either way.

## #93 — log-driven fix: Windows confirm dialogs blocked by ACL

GitHub #93 (English) / GitLab #12 (Korean). Found by reading GlitchTip (issue 11, Windows v0.14.1), not a user report: `Command plugin:dialog|confirm not allowed by ACL`. On WebView2 `window.confirm` routes through the dialog plugin; the capability only granted default/save/open, so all delete confirmations silently failed on Windows. Added `dialog:allow-confirm/ask/message` to `capabilities/default.json`. Also in the logs: a Linux GTK init panic (issue 12, AppImage on a machine where GTK failed to initialize) — tracked, not yet actionable without more context; and the maintainer's clean wt-ime-diag2 baseline (n229=0). No M5 traces exist after v0.13.5 — diag was removed in v0.13.6 and v0.14.2 (which re-adds it) is still in CI, so the M5 "still broken" report was against ≤0.14.1 (writingsuggestions only, no suppression).

## #83 — maintainer-Mac Korean breakage root-caused with LOCAL diag; latch fix

- The maintainer's own Mac lost Korean input — in the **installed app** (auto-updated to 0.14.1 at 10:27), while a freshly launched local binary worked. Burned GlitchTip send budgets on startup noise again, so the diag now ALSO accumulates into `localStorage["wt.ime.local-diag"]` (no cap) — readable straight from the WKWebView sqlite (`~/Library/WebKit/<app id>/WebsiteData/.../LocalStorage/localstorage.sqlite3`; note: the bare release binary uses `WebKit/wowterminal/`, the installed bundle `WebKit/com.wowterminal.app/`).
- Local diag for typing "안녕하세요": `n229:33, nCS/nCU/nCE:0, mirrorChars:36, mirrorBs:31, bsSuppressed:0` → this Mac's Korean is pure keyCode-229 + mirror; composition events never fire; the v0.14.2 ASCII suppression never triggers on Hangul (safe).
- Therefore the installed-app breakage is the **`nativeComposition` permanent latch**: one stray composition event (press-and-hold accent popup, dictation, …) flips it forever, killing the mirror that this machine requires — Korean stays broken until restart. Exactly matches "fresh launch works".
- Fix: the switch now stays true only while a composition is in progress (released ~80ms after `compositionend`); environments that compose everything keep re-entering composition per keystroke, so their native path is effectively unchanged.

## Release v0.14.3
- Patch (fix-only: #83 composition-window latch fix, #93 Windows dialog ACL). 0.14.2 → 0.14.3 in the 4 version files. Tag `v0.14.3` → CI → auto-publish. (v0.14.2, which had been left in draft, was published first.)

## #83 — new report: deleting Hangul leaves 2-space gaps (jh.kim's Mac); diag read FIRST, content ring added (v0.14.4)

- Read the logs before touching code this time. jh.kim's v0.14.3 trace: `n229:11, nCS/nCU/nCE:0, mirrorBs:11, mirrorChars:11, onData:0, bsSuppressed:0` → 229-only mirror Mac; the v0.14.3 composition-window change is irrelevant (no composition events) and the v0.14.2 suppression never fired. Fresh install (no hosts) → likely never-worked machine class (M5-like), not a regression.
- bs:chars:229 exactly 1:1:1 with stray standalone jamo + double-space gaps on screen → leading suspicion: the hidden textarea's own composition is broken on this machine class (jamo+spaces fed in), and the mirror faithfully relays it. Counters can't show content, so the diag now keeps a 14-entry ring of mirror transitions (imeSent→value, typed content included — temporary, removed at #83 close) sent with wt-ime-diag2.

## Release v0.14.4
- Patch (diagnostic only). 0.14.3 → 0.14.4. Tag → CI → auto-publish. jh.kim/M5 users type Hangul + delete on 0.14.4; the ring will show the exact textarea transitions.
