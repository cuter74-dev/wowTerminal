# 2026-05-28 작업 로그

기획서(`http://dev.brainsp.com:5173/.../render`, 71개 와이어프레임)를 받아 카테고리 단위로 구현 진행.
우선순위는 사용자 결정에 따라 **메인 터미널 → SSH → AI → SFTP** 순. 모든 작업은 `gh` 미설치로 GitHub Issue 없이 work-log + 커밋으로 추적.

## S-017 — SSH 호스트 그룹/태그 관리

### 무엇을
- 백엔드: `Group`/`Tag` 모델(`ssh/types.rs`), `GroupStore`(`groups.toml`) / `TagStore`(`tags.toml`), CRUD 커맨드 6개
- `SshHost`에 `group_id: Option<String>` 추가 (`#[serde(default)]`로 기존 hosts.toml 호환)
- 프론트: `GroupTagManager` 모달(그룹 flat 리스트 + 태그 6색 팔레트), `HostForm`에 그룹 드롭다운 + 태그 토글, `HostList`를 그룹별 섹션으로 묶고 태그 chip(색상 배경) 표시

### 왜 / 메모
- v1은 flat 그룹 (와이어프레임의 3단계 중첩 + 드래그 정렬은 후속). 색상은 미리 정의된 6색.
- 그룹 삭제 시 소속 호스트의 `group_id`만 해제(호스트 보존), 태그 삭제 시 호스트 `tags`에서 이름 제거 — 데이터 정합성을 백엔드 커맨드에서 처리.

---

## SSH 인증 완성 — agent 위임 + password prompt + Keychain 자동 저장

기획서대로 실제 SSH 접속을 시키는 과정에서 막힌 것들을 순서대로 해결. (work-log #5에 적힌 "v1 인증 미완" 한계 해소)

### 1. `ssh_connect` 인자 매핑 버그
- 증상: `missing field 'host_id'`. Tauri 2는 함수 인자 최상위 이름만 camel↔snake 변환하고 **struct 내부 필드는 serde 그대로** 받음.
- 수정: `SshConnectArgs`에 `#[serde(rename_all = "camelCase")]`.

### 2. ENETUNREACH (os error 51)
- 증상: `connect: Network is unreachable`. 진단 로그로 확인 결과 사용자가 입력한 포트(10122)가 실제로 도달 불가였음 (시스템 ssh의 `-P`는 PKCS#11 옵션이라 포트가 아니라 기본 22로 붙었던 것). 포트 22로 바꾸니 TCP는 연결.
- 보강: `tokio::net::lookup_host`로 명시적 resolve 후 IPv4 우선 정렬해서 연결 — 일부 환경의 IPv6 우선/fallback 문제 예방.

### 3. ssh-agent 위임 인증 (`ResolvedAuth::Agent`)
- `russh::keys::agent::client::AgentClient`로 `$SSH_AUTH_SOCK` 연결 → `request_identities` → 각 `AgentIdentity::PublicKey`로 `authenticate_publickey_with(user, pubkey, None, &mut agent)` 시도. 첫 성공이면 통과.
- 인증서(Certificate) 자격증명은 v1 미지원(건너뜀). agent에 키 없으면 명확한 에러.

### 4. Password prompt (즉석 입력, 저장 안 함)
- `SshAuthMethod::PasswordPrompt` variant + `SshError::PasswordRequired{host,port,user}` + `SshConnectError::PasswordRequired`.
- `ssh_connect` args에 `password: Option<String>`. `resolve_auth`가 PasswordPrompt인데 password 없으면 `PasswordRequired` 반환.
- 프론트: `<PasswordPromptModal>` (🔐) — 메모리에서만 사용, retry 흐름은 leaf별 `passwordByLeaf` + `retryNonce`로 재마운트.

### 5. Keychain 자동 저장
- `lib.rs`에서 `KeyringStore("wowterminal")`를 기본 secret store로 활성화 (macOS Keychain / Windows Credential Manager / Linux Secret Service).
- `ssh_remember_password(hostId, password)` 커맨드: keyring에 저장 + 호스트 auth를 `Password{secret_id=hostId}`로 갱신 → 다음 접속부터 자동 인증.
- 프론트: PasswordPromptModal에 "OS Keychain에 저장" 체크박스. `Terminal.onSshConnected` 콜백으로 **접속 성공 후에만** 저장(잘못된 비밀번호는 저장 안 됨). 저장 후 메모리의 password/remember 즉시 클리어.

### 한계
- Linux headless(키링 데몬 없음)는 저장 실패 → `EncryptedFileStore`(이미 구현됨) 폴백을 `CompositeStore`로 묶는 작업이 후속.
- 호스트 삭제 시 keychain 항목 자동 정리는 미구현.

---

## AI 채팅 (S-038/043/046/047/048/049/050)

기존 `OpenAiCompatibleBackend` 어댑터 위에 등록/설정/채팅 UI를 올림.

### S-046/050 — 채팅 패널 + 백엔드 등록
- 백엔드: `BackendsStore`(`backends.toml`), `ai_save_backend`/`ai_delete_backend`/`ai_list_backend_configs`. API 키는 keyring(`wowterminal-ai`)에 저장, toml엔 `api_key_secret_id`만. 앱 시작 시 `bootstrap_registry`로 자동 등록.
- 프론트: `<LlmSetupModal>` (프리셋: OpenAI/Ollama/직접입력), `<AIPanel>` 실 채팅 UI (모델 드롭다운, 메시지 버블, 입력창, 빈 상태).

### chat_url 중복 버그
- 증상: `http 404: {"detail":"Not Found"}`. 사용자 `api_base`가 이미 `/v1/chat/completions`로 끝나는데 어댑터가 또 `/chat/completions`를 붙임.
- 수정: `chat_url()`이 이미 `/chat/completions`로 끝나면 그대로 사용.

### S-047/048 — 컨텍스트 자동 첨부 + 명령 제안 카드
- `terminalRegistry` (모듈 레벨 Map): 각 leaf의 `getRecentText`/`sendInput`을 등록. `Terminal`이 mount 시 등록, unmount 시 해제.
- 컨텍스트: 전송 시 포커스된 패널의 최근 60줄을 system 메시지로 첨부 (체크박스로 on/off, 화면 표시 메시지엔 미포함).
- 명령 카드: AI 응답의 \`\`\`코드블록\`\`\`을 추출해 카드 + "▶ 터미널에 입력"(개행 없이 입력만 — 실행은 사용자가 Enter) / "복사".

### S-049 — 대화 이력
- `chatSessions.ts`: localStorage 기반 다중 세션 (id/title/messages/backendId/updatedAt). 첫 user 메시지로 제목 자동 생성.
- `<AIPanel>`에 🕘 이력 드로어(세션 목록/전환/삭제) + ＋ 새 대화. 매 전송마다 활성 세션 저장.

### 미완 (후속)
- S-051~053 (인라인 자동완성 / AI 명령 제안 드롭다운 / Ctrl-R 히스토리) — xterm 깊은 통합 필요.
- 스트리밍 응답 (`complete_stream`), 컨텍스트 토큰 길이 관리.

---

## 검증
- `cargo test --lib` ✅ **40 passed** (회귀 없음)
- `npm run build` ✅ (번들 ~608KB / gzip ~168KB)
- 수동 GUI: 실제 SSH 호스트(password→keychain)로 접속, AI 백엔드 등록 후 채팅·컨텍스트·명령카드·이력 동작 확인.

---

## LLM 백엔드 버그픽스 (추가 등록 안 됨 / 404)

### 추가가 안 되고 기존 게 변경됨
- 원인: 백엔드 식별자가 **id**인데 같은 프리셋을 두 번 고르면 id가 같아(`openai`) `upsert`가 덮어씀.
- 수정: 새 백엔드 저장 시 id 중복이면 거부 + 안내. 프리셋 선택 시 충돌하면 자동 `-2`,`-3` suffix(`existingIds` 기반).

### `http 404: {"detail":"Not Found"}`
- 원인: 사용자가 `api_base`에 `/v1/chat/completions`까지 + **끝에 공백**을 넣어 `ends_with("/chat/completions")` 체크가 false → `/chat/completions` 중복 부착.
- 수정: 어댑터 `new`에서 `.trim().trim_end_matches('/')`, 폼 저장 시 `displayName/apiBase/defaultModel` trim. 어댑터가 로드 시 정리하므로 기존 toml 값도 자동 교정.
- 참고: macOS의 `dirs::config_dir()`는 `~/Library/Application Support`(`~/.config` 아님) — 설정 파일 위치 확인 시 주의.

---

## S-025/033/034/028 — SFTP 파일 브라우저

기존 SSH 연결 코드를 재사용하기 위해 `SshSession::connect`에서 **핸드셰이크+인증 부분을 `establish()`로 추출**. PTY 세션과 SFTP가 공유.

### 백엔드 (`ssh/sftp.rs`, `russh-sftp 2.3`)
- `SftpManager`: 호스트 ID별 SFTP 연결 캐시. `establish` → `channel_open_session` → `request_subsystem("sftp")` → `SftpSession::new(channel.into_stream())`.
- 리스팅: `list_dir`(read_dir → FileEntry{name,is_dir,size,modified,permissions}), `canonicalize`.
- 전송: `download`(open→read_to_end→로컬 write), `upload`(로컬 read→create→write_all). 
- 파일 작업: `remove`(file/dir), `rename`, `mkdir`.
- 커맨드: `sftp_open`/`sftp_list`/`sftp_disconnect`/`sftp_download`/`sftp_upload`/`sftp_remove`/`sftp_rename`/`sftp_mkdir`, 로컬은 `local_list_dir`(std::fs, unix permissions mode).

### 프론트 (`<FileBrowser>`)
- 타이틀바 **📁 파일** 버튼(활성 패널이 SSH일 때만) → 이중 패널 모달 (좌 로컬 / 우 원격).
- 디렉토리 더블클릭 탐색 + `..` 상위, 파일 클릭 선택(하이라이트), 디렉토리 먼저 정렬.
- 가운데 ← 다운로드 / → 업로드 (파일 선택 시 활성), 전송 중 표시.
- 툴바: 원격 새 폴더 / 원격 삭제 / 새로고침.

### 한계 / 후속
- SFTP는 v1에서 **password-prompt 인증 미지원** — agent 또는 keychain 저장된 호스트만 (resolve_auth에 password=None 전달).
- 전송 진행률 바(S-033/034 풀)·전송 큐(S-035)·충돌 확인(S-036)·우클릭 컨텍스트 메뉴(S-028)·권한 편집(S-029)·미리보기(S-030)·검색(S-031)은 후속. rename 백엔드는 있으나 UI 미연결.
- 대용량 파일은 메모리에 전체 로드(read_to_end/read) — 스트리밍 청크 전송은 후속.

## 다음 단계 후보
- S-019~024 SSH 키 관리 (생성/import/상세/삭제 + 키스토어)
- S-029~037 SFTP 나머지 (권한/미리보기/검색/전송 큐/충돌/진행률)
- S-051~053 인라인 자동완성 / Ctrl-R
- S-054~059 설정, S-060~071 공통 상태/에러 화면
- 멀티 윈도우 세션 인계, Linux headless secret 폴백, `gh` 설치 후 이슈 소급 생성
