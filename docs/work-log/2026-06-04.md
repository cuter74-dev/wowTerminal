# 2026-06-04 작업 로그

> Warp/cmux 벤치마킹 기능 백로그(#55~#65) 일괄 구현 시작. 배치별로 진행.

## #56 — bash 로컬 셸 통합(OSC 133)

### 무엇을 / 왜 / 어떻게
- 기존 zsh만 OSC 133 주입 → bash도 지원. pty/manager.rs setup_bash_integration: temp에 wt-bashrc.bash 작성(사용자 ~/.bashrc source + DEBUG trap=preexec C, PROMPT_COMMAND=precmd D;$?, 프롬프트당 C 1회 가드) 후 `--rcfile`로 주입. spawn에서 shell=bash면 호출.

## #55 — 긴 명령 완료 알림 + 탭 배지

### 무엇을 / 왜 / 어떻게
- 백엔드: tauri-plugin-notification 추가(Cargo + lib.rs init + capability notification:default), npm @tauri-apps/plugin-notification.
- 버스: commandBus.ts(emitCommandDone/onCommandDone). Terminal OSC 133 D에서 emitCommandDone({paneId,durationMs,exit}).
- App: 구독 → durationMs>=8000 && 백그라운드(탭≠활성 || !document.hasFocus())면 setTabAlerts + sendNotification(title=탭 라벨, body=✓/✗ + 시간). 권한 요청. 활성 탭 되면 배지 해제(effect).
- TabBar: alertedTabIds로 주황 점 배지. tabsRef/activeTabIdRef로 최신값 참조(리스너 1회 등록).
- 임계값 8초. 보고 있는 탭이면 알림 안 함.

## #57 — 명령 팔레트(⌘K)

### 무엇을 / 왜 / 어떻게
- CommandPalette.tsx: 검색 입력 + 퍼지 매칭(부분 순서) 리스트, ↑↓/Enter/Esc, 바깥 클릭 닫기. PaletteItem{id,label,hint,action}.
- App: ⌘K/Ctrl-K로 토글(onKey 맨 앞, 편집 중에도). paletteItems = [새 로컬 탭, 설정] + 호스트별 접속(newSshTab) + 최근 명령 30개(getHistory 역순 → 활성 pane에 sendInput, Enter는 사용자). PAL_STR 11개 언어.

## #59 — 입력 브로드캐스트

### 무엇을 / 왜 / 어떻게
- terminalRegistry: broadcastEnabled 플래그 + setBroadcastEnabled/isBroadcastEnabled + broadcastInput(origin, text)(origin 제외 모든 등록 터미널에 sendInput → PTY 직접 쓰기라 onData 재진입 없음=루프 없음).
- Terminal: onData·IME 미러에서 입력을 broadcastInput(paneId, data). 대상 패널은 셸 에코로 표시.
- App: broadcast state → useEffect로 setBroadcastEnabled 동기화. TitleBar에 broadcast/onToggleBroadcast 전달.
- TitleBar: 📡 토글 버튼(켜짐=빨강 강조). broadcast 라벨 11개 언어.
- 참고: 모든 등록 패널(백그라운드 탭 포함)로 감 — 빨간 표시로 경고. 후속: 가시 패널만 옵션.

## #58 — 명령 스니펫

### 무엇을 / 왜 / 어떻게
- snippets.ts: localStorage(wt.snippets) load/save. Snippet{id,name,command}.
- 설정에 'Snippets' 탭(SnippetsTab): 이름+명령 입력으로 추가, 목록 표시·삭제. tabSnippets 등 7개 키 11개 언어.
- 명령 팔레트(App): loadSnippets()를 paletteItems에 포함 → 활성 pane에 sendInput. PAL_STR snippet 라벨 11개 언어.

## #66 — 터미널 색 테마 추가

### 무엇을 / 왜 / 어떻게
- settings.ts: ThemeColors 인터페이스(background/foreground 필수 + cursor·selection·ANSI 16색 선택). TERMINAL_THEMES에 Dracula·Nord·Solarized Dark·Monokai 풀 팔레트 추가. theme 타입에 4종 추가. TERMINAL_THEME_LIST(키+표시명) 드롭다운용.
- SettingsModal: 터미널 탭 테마 드롭다운을 TERMINAL_THEME_LIST.map으로 렌더(dark/light는 번역, 나머지는 고유명).
- Terminal: 이미 TERMINAL_THEMES[theme]를 xterm theme로 그대로 전달 → ANSI 팔레트 자동 반영. 컨테이너 배경도 theme.background 사용.

## #67 — 세션 로깅

### 무엇을 / 왜 / 어떻게
- src/sessionLog.ts(신규): stripAnsi(OSC/CSI/ESC/제어문자 제거, 개행·탭 보존, RegExp 생성자+\x 이스케이프로 raw 제어문자 회피) + SessionLogger 클래스(스트리밍 TextDecoder로 바이트 누적, 1.5초 디바운스·16KB 임계 flush, session_log_append invoke).
- src-tauri ssh/commands.rs: session_log_append(dir,name,content) — dir 비면 ~/wowterminal-logs, create_dir_all, name의 '/'·'\\' 치환으로 경로 탈출 방지, OpenOptions append. lib.rs 등록.
- Terminal.tsx: loggerRef + termSettings.logging 토글 effect(on=생성/off=dispose), 언마운트 flush. 출력 핸들러에서 term.write와 함께 loggerRef.append(bytes).
- settings.ts: terminal.logging/logDir 추가, 기본 off/"".
- SettingsModal: 터미널 탭에 로깅 토글 + (켜짐 시)로그 폴더 입력 + '선택…' 디렉터리 다이얼로그. logging/logDir/chooseDir 11개 언어.

## #68 — 세션 대시보드

### 무엇을 / 왜 / 어떻게
- SessionDashboard.tsx(신규): 모달 표(세션/소스/상태/명령 수/마지막 명령). 행 클릭 시 onJump→탭 활성화. en+ko 사전(나머지 en 폴백), fmtDur/fmtAgo 헬퍼.
- App: sessionStats state(paneId→{count,lastExit,lastDurationMs,lastAt}) — commandBus 구독에서 모든 명령마다 누적(알림 8초 임계는 그대로). dashRows useMemo(tabs×collectLeaves + sessionByLeaf + sessionStats + labelForHost). dashboardOpen state, 팔레트 액션 'act-dashboard', 모달 렌더.
- TitleBar: 📊 버튼(onOpenDashboard) + dashboard 11개 언어. PAL_STR에 dashboard 키 추가.

## #69 — 원격 파일 빠른 편집

### 무엇을 / 왜 / 어떻게
- Rust: ssh/sftp.rs write_text(create→truncate, write_all), ssh/commands.rs sftp_write_text, lib.rs 등록.
- FileBrowser: preview 상태에 editable/path/hostId/isLocal 추가. previewRemote/previewLocal 텍스트 분기에서 editable=true + 위치정보 세팅(로컬은 local_write_text, 원격은 sftp_write_text).
- PreviewModal: canEdit이면 textarea+draft 상태, dirty(●) 표시, 💾 저장 버튼·⌘S, 저장중/저장됨/실패 상태. save/saved/saveErr/editFooter 11개 언어.

## #70 — 포트 포워딩 (-L / -D)

### 무엇을 / 왜 / 어떻게
- Rust ssh/tunnel.rs(신규): TunnelManager. start_local(전용 핸들 establish→TcpListener bind→accept마다 channel_open_direct_tcpip + copy_bidirectional), start_dynamic(SOCKS5 핸드셰이크 직접 구현: greeting/no-auth, CONNECT 요청 파싱 ipv4/domain/ipv6, 목적지로 direct-tcpip, 성공/실패 응답), stop(AbortHandle), list. handle은 Clone 불가 → Arc로 공유.
- commands.rs: tunnel_start_local/tunnel_start_dynamic/tunnel_stop/tunnel_list, SshState.tunnel + build_state. lib.rs 등록.
- 프론트 PortForwardModal.tsx(신규): 활성 터널 목록(중지) + 새 터널 폼(호스트/종류/로컬포트/원격호스트·포트), tunnel_list/start/stop invoke. en+ko.
- App: portForwardOpen state + 팔레트 'act-portforward'(PAL_STR portForward 11개 언어) + 렌더.
- 한계: 원격(-R)·비밀번호 프롬프트 인증은 후속. 인증은 agent/키체인 호스트.

## #71 — 점프 호스트 (ProxyJump)

### 무엇을 / 왜 / 어떻게
- session.rs: make_handler/auth_handle로 establish 리팩터(공용화). establish_via 추가 — 점프 핸들 establish→channel_open_direct_tcpip(target)→client::connect_stream로 대상과 SSH 협상. (target_handle, jump_handle) 반환. connect()에 jump: Option<JumpSpec> 추가, jump_keepalive를 actor 태스크로 move해 세션 동안 유지(drop 시 터널 붕괴 방지). JumpSpec 구조체.
- manager.rs connect: jump 인자 추가·전달.
- types.rs: SshHost.proxy_jump(serde default). store.rs sample 갱신.
- commands.rs ssh_connect: proxy_jump 해석→점프 호스트 로드+resolve_auth(agent/키체인)→JumpSpec.
- 프론트 types.ts SshHost.proxy_jump. HostForm: ssh_list_hosts 로드, '점프 호스트' 드롭다운(자기 제외), payload.proxy_jump. proxyJump/proxyDirect 11개 언어.
- 테스트: 기존 connect 호출에 None 추가. ssh 테스트 21 pass.
- 한계: SFTP ProxyJump·다단계·점프용 비밀번호 프롬프트는 후속.

## #72 — 타이틀바 아이콘 정리 (이모지→SVG)

### 무엇을 / 왜 / 어떻게
- TitleBar: 📊→DashboardIcon(Feather 'layout' rect+분할선), 📡→BroadcastIcon(Feather 'radio' 중앙 점+전파 호). 설정 톱니와 동일한 24px·strokeWidth 2·round 스타일 인라인 SVG. 버튼은 flex+padding으로 정렬, 브로드캐스트 활성 빨강 강조 유지.
- flaticon 직접 사용은 라이선스/출처표기 이슈라 동일 스타일 자작 SVG로 대체.

## #73 — macOS 코드 서명 + 공증 활성화

### 무엇을 / 왜 / 어떻게
- Apple Developer 계정(Brain OS Institute, Team J5C9SY326P)에서 Developer ID Application 인증서 발급 → 키체인 설치 → .p12 내보내기.
- GitHub Secrets 6개 등록: APPLE_CERTIFICATE(.p12 base64), APPLE_CERTIFICATE_PASSWORD, APPLE_SIGNING_IDENTITY("Developer ID Application: Brain OS Institute (J5C9SY326P)"), APPLE_ID, APPLE_PASSWORD(앱 전용 암호), APPLE_TEAM_ID(J5C9SY326P).
- release.yml: 메인 'Build & release'와 dmg best-effort 스텝 양쪽 env에 APPLE_* 6개 추가 → tauri-action이 .app/.dmg 서명 + notarytool 공증. releaseBody macOS 문구를 'signed + notarized'로 변경.
- docs/guide/release-signing.md: macOS 구성 완료 상태로 갱신.
- 효과: 다음 태그부터 다운로드 시 "손상됨" Gatekeeper 경고 사라짐(공증 통과). Windows는 여전히 unsigned.

## v0.10.0 배포 + macOS 서명/공증 트러블슈팅

### 무엇을 / 왜 / 어떻게
- v0.10.0 배포(파워유저 11종 #54~#72 + 서명/공증 #73). 버전 4곳 올림, 태그 푸시→Actions 빌드.
- "wowTerminal.app 손상됨" Gatekeeper 경고 = 미서명+격리속성. Developer ID(Brain OS Institute, J5C9SY326P) 서명+공증으로 근본 해결.
- 빌드 실패 "failed to resolve signing identity"(+"1 identity imported") 2회 → 원인은 .p12에 Developer ID G2 중간 인증서 누락. 키체인에서 인증서+중간인증서 함께 .p12 내보내기(4.5KB)로 해결. APPLE_CERTIFICATE/PASSWORD 갱신 후 "Re-run failed jobs"(PAT로 CLI rerun 불가).
- latest.json에 darwin-aarch64+x86_64 확인 후 draft 자동 publish(--latest).
- docs/guide/release-signing.md에 중간인증서 함정 트러블슈팅 섹션 추가. 메모 reference_macos_signing 저장.

## #73 — 포트 포워딩 원격(-R)

### 무엇을 / 왜 / 어떻게
- session.rs: RemoteForwards 타입(Arc<Mutex<HashMap<bind_port,(host,port)>>>) + TofuShared.remote_forwards. Handler에 server_channel_open_forwarded_tcpip 구현 — 서버가 연 forwarded-tcpip 채널을 라우팅표에서 찾은 클라이언트 측 대상으로 TcpStream 연결 후 copy_bidirectional. establish→establish_inner 리팩터 + establish_with_forwards 추가. establish_via는 빈 forwards로 호출.
- tunnel.rs: start_remote — forwards 표에 (bind_port→대상) 등록 → establish_with_forwards → handle.tcpip_forward(bind)로 서버 리스닝 요청(실제 바인드 포트 반환) → 핸들/표를 pending 태스크로 잡아 유지(abort 시 정리).
- commands.rs: tunnel_start_remote(host_id, bind_host, bind_port, local_host, local_port, password). lib.rs 등록.
- PortForwardModal: 종류 드롭다운에 '원격(-R)', remote일 때 라벨 분기(바인드 포트/로컬 대상 호스트·포트), start() remote 분기(localPort=bind_port, remoteHost/remotePort=대상), 활성 터널 표시 R · server→target. remote/bindPort/targetHost/targetPort en+ko.
- 테스트: cfg(test) make_handler에 remote_forwards 추가. ssh 21 pass.
- 참고: 비밀번호 프롬프트 인증·다단계는 여전히 후속. 서버 GatewayPorts 설정에 따라 외부 바인드는 제한될 수 있음(기본 127.0.0.1 바인드).

## #74 — 호스트 폼 포트 입력 버그

### 무엇을 / 왜 / 어떻게
- HostForm: port를 number→문자열 상태로(편집 중 빈 값 허용). input type=number→text+inputMode=numeric, onChange는 숫자만 필터(빈 값 허용), placeholder 22. 저장 시 portNum=clamp(parseInt||22, 1..65535)로 변환.

## #75 — 한글 IME 조합 중 이중 표시

### 무엇을 / 왜 / 어떻게
- 증상: 한글 조합 중 음절이 두 번 찍힘(찾→차차, 수정→수정정). 확정/계속 입력 시 정상화. 화면 기록(.mov)을 ffmpeg로 프레임 추출해 입력 줄 몽타주로 확인.
- 원인: 최신 WKWebView가 composition 이벤트를 발생시켜 xterm이 .composition-view 오버레이로 조합 중 글자를 추가 렌더. 우리 IME textarea 미러는 이미 조합 텍스트를 PTY로 보내 셸/TUI가 직접 표시 → 오버레이가 겹쳐 이중.
- 수정: App.css에 `.xterm .composition-view{display:none!important}`. 미러가 PTY 표시를 담당하므로 오버레이는 불필요한 중복. 사용자 확인 완료.

## #76 — 앱 실행 아이콘 교체

### 무엇을 / 왜 / 어떻게
- dev/release 아이콘 불일치 + 구버전 → 새 블루 'WT' 터미널 로고로 통일.
- `npm run tauri icon image22.png`(1254x1254)로 src-tauri/icons/ 전체 재생성: icon.icns(macOS), icon.ico(Windows), 32/64/128/128@2x png, Square*Logo(Windows Store), iOS/Android 세트.
- 적용: 아이콘은 빌드 시 번들에 임베드 → dev는 재빌드, release는 다음 태그 빌드부터 반영. 기존 설치본은 업데이트 설치 시 갱신. macOS 아이콘 캐시 때문에 즉시 안 바뀌면 Dock 재시작/캐시 클리어 필요.
