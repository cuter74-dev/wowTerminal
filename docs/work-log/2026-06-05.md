# 2026-06-05 작업 로그

## #80 — 로컬 셸 PATH 누락 (비로그인 셸이 ~/.zprofile 스킵)

### 무엇을 / 왜 / 어떻게
- 증상: 사용자 화면에 .zshrc:116/130 command not found: pyenv/jenv + 명령이 안 먹음. v0.10.0.
- 원인: pty/manager.rs가 셸을 비로그인으로 spawn. macOS Terminal.app은 로그인 셸 → /etc/zprofile(path_helper)·~/.zprofile 읽음. Homebrew 공식 설치가 brew shellenv를 ~/.zprofile에 넣으므로 비로그인은 Homebrew/그로 깐 도구 PATH가 통째로 빠짐 → command not found.
- 검증: `/bin/zsh -c 'echo $PATH'`(비로그인) vs `/bin/zsh -l -c ...`(로그인) — 로그인만 /etc/zprofile path_helper 반영. /etc/zprofile에 path_helper 확인.
- 수정: zsh `cmd.arg("-l")`. 기존에 만들던 임시 .zprofile(사용자 .zprofile source)이 비로그인이라 dead였는데 -l로 실제 로드됨. bash는 로그인이면 --rcfile 무시되므로 비로그인 유지하되 rcfile에서 /etc/profile + ~/.bash_profile/.bash_login/.profile를 source해 PATH 확보.
- pty 테스트 4 pass.

## #81 — 패널 토글 단축키 + 터미널 파일 드래그 업로드

### 무엇을 / 왜 / 어떻게
- 단축키: settings.ts ShortcutAction에 toggleHostPanel/toggleAiPanel 추가, DEFAULT_KEYBINDINGS ⌘B/⌘J. App onKey에 matchesBinding 처리. toggleHostPanel/AiPanel을 setSettings 함수형+saveSettings로 변경(키 핸들러 stale 클로저 안전). SettingsModal ACTION_LABELS·SHORTCUT_ACTIONS에 추가(en/ko).
- 드래그 업로드: manager.rs 로컬 zsh/bash precmd 훅에 OSC7(file://host/PWD) 보고 추가 → cwd 추적(파일 드롭 위치). ssh/commands.rs local_copy_into(src,dir) 추가, lib.rs 등록. App: getCurrentWebview().onDragDropEvent → 포커스 pane 대상. 로컬=local_copy_into(현재 cwd), SSH=sftp_open(연결 보장)→sftp_upload(원격 cwd, 모르면 ".")→결과 경로 입력 삽입. 파일 브라우저 열림 시 스킵(ref 가드). quote()로 공백 경로 처리.
- 한계: SSH 원격 cwd는 원격이 OSC7 emit해야 정확(기본은 홈). pty 테스트 4 pass.

## #81 후속 — 드래그 업로드 마감(SSH cwd 자동주입 + 프롬프트 수정)
- App 드롭 핸들러: 경로 입력 삽입 제거 → 데스크톱 알림 피드백(⬆/📄/✗).
- session.rs: SSH 접속 0.5초 후 원격 셸에 OSC7 cwd 훅 1줄 주입(REMOTE_OSC7_SETUP, zsh/bash 대응) + 즉시 1회 emit + Ctrl-L(\x0c)로 화면 정리. 앞 공백으로 히스토리 회피.
- Terminal.tsx: OSC7 수신 핸들러 return false→true. false면 OSC7 수신 후 xterm 파서가 다음 프롬프트 바이트를 삼켜 로컬 셸에서 명령 후 프롬프트가 안 뜨던 회귀 수정(pty 테스트로 셸은 정상 emit 확인).
- 빌드/40 테스트 통과.
