# 2026-06-02 작업 로그

## #39 — 새 탭 접속 시 다른 세션 출력이 새어 상단에 엉뚱한 프롬프트가 찍힘

### 무엇을 / 왜 / 어떻게
- 증상: 호스트 목록에서 새 탭으로 SSH 접속하면 상단에 다른 서버/세션의 프롬프트가 찍히고 그 뒤로 새 서버 배너가 이어짐(예: mscllm 새 탭에 `cuter74@testllm:~$`가 보임). 그동안 추적하던 "상단 프롬프트 잔상"의 진짜 근본 원인.
- 원인: `ssh:output`/`pty:output`은 **모든 세션 공유 전역 이벤트**. `Terminal`의 출력 리스너 필터가 `if (sessionId && payload.session_id !== sessionId) return`이라, 새 탭 spawn이 끝나기 전(`sessionId === null`)엔 조건이 false가 되어 **다른 활성 세션의 출력까지 이 터미널에 써졌음**. 마침 다른 탭에서 Claude Code가 활발히 출력 중이라 그 프롬프트가 새 탭 상단에 샜다.
- 수정: 필터를 `if (!sessionId || payload.session_id !== sessionId) return`로 변경 — sessionId 설정 전엔 아무것도 쓰지 않음. `sessionId = await invoke(spawnCmd...)`는 await 직후 동기 할당이라(이벤트 콜백은 이후 태스크에서 처리) 자기 세션의 배너/출력 유실은 없음.

### 메모
- 이전에 "상단 프롬프트"를 OSC 7 주입 탓으로 보고 주입을 제거(#37)했는데, 그건 곁가지였고 실제 근본 원인은 이 전역 출력 필터 버그였다. (OSC 7 주입 제거 자체는 별개로 유지 — cwd 동기화는 ~/.bashrc 훅으로 안내.)

## #40 — tmux 재접속 시 화면이 아주 좁은 폭으로 깨짐 (숨김 탭 0크기 fit)

### 무엇을 / 왜 / 어떻게
- 증상: 여러 testllm 탭이 같은 원격 tmux 세션에 attach된 상태에서 tmux 내용(Claude Code)이 ~10칸 폭으로 줄바꿈돼 세로로 깨져 보임.
- 원인: 배경(숨김) 탭은 display:none → 컨테이너 0 크기. 그때 `ResizeObserver`가 `fit()` 호출 → cols/rows가 최소값(~2)으로 줄어 원격 PTY로 전달. tmux는 attach된 클라이언트 중 **가장 작은 크기**에 맞추므로, 한 숨김 탭의 tiny 크기가 공유 세션 전체를 쪼그라뜨림.
- 수정: `safeFit()` 도입 — 컨테이너 `clientWidth/Height >= 8`일 때만 fit. 적용 지점: 초기 fit, ResizeObserver, finishAttach(이후 resize도 가드), 설정 변경 effect, 등록된 `fit()` 메서드.
- 보완(원격 권장): tmux `set -g window-size latest`로 가장 작은 게 아닌 최근 활성 클라이언트에 맞추게.

### 현재 깨진 세션 복구
- 남는 testllm 탭을 닫아 클라이언트를 줄이거나, 각 탭을 한 번씩 포커스해 재fit, 또는 tmux에서 `tmux resize-window -A`.

## #41 — 설정 내보내기/가져오기를 JSON 파일로

### 무엇을 / 왜 / 어떻게
- 기존: 백업이 텍스트박스 + 클립보드 복사/붙여넣기 방식. → 사용자가 JSON 파일로 직접 저장/불러오기 원함.
- 추가:
  - 백엔드 `local_write_text(path, content)` 커맨드 신설(ssh/commands.rs) + lib.rs 등록. 읽기는 기존 `local_read_text` 재사용.
  - 프론트(SettingsModal BackupTab): `@tauri-apps/plugin-dialog`의 `save`/`open`로 네이티브 다이얼로그. "파일로 내보내기"→save 다이얼로그→`local_write_text`, "파일에서 가져오기"→open 다이얼로그→`local_read_text`→파싱·적용.
  - export JSON 빌드/적용 로직을 `buildExportJson`/`applyImportJson`로 분리해 텍스트 방식과 파일 방식이 공유.
  - 기존 텍스트(붙여넣기)/클립보드 버튼은 보조로 유지.
  - 11개 언어 라벨 추가(exportFile/importFile/exportFileDone), capability에 `dialog:allow-save`/`allow-open` 명시.
- 시크릿(비밀번호/키)은 export에 미포함(기존과 동일).
- 주의: Rust 커맨드 추가라 dev 재빌드 필요(HMR 불가).

## #42 — 터미널 Tab 키가 AI 패널로 포커스를 넘김

### 무엇을 / 왜 / 어떻게
- 증상: 터미널에서 Tab을 누르면 가끔 포커스가 AI 어시스턴트 영역으로 이동.
- 원인: 인라인 자동완성 제안이 떠 있을 때의 Tab 분기가 `return false`만 하고 `e.preventDefault()`를 안 호출 → xterm은 처리 안 하지만 브라우저 기본 Tab(포커스 traversal)이 그대로 실행돼 다음 포커스 대상(AI 패널 textarea)으로 넘어감. 제안 없을 땐 xterm이 처리하며 preventDefault해서 안 샜기에 "가끔"으로 보였음.
- 수정: `customKeyEventHandler`에서 Tab(metaKey/ctrlKey/altKey 없음)을 포괄 처리 — 항상 `e.preventDefault()`. 제안 있으면 수락, 없으면 셸로 `\t`(Shift+Tab은 역탭 `\x1b[Z`) 전달 후 return false. 포커스 이동 원천 차단.

## #43 — 터미널 우클릭 컨텍스트 메뉴

### 무엇을 / 왜 / 어떻게
- Terminal.tsx 루트 div에 `onContextMenu`(preventDefault + 위치 저장) → `TerminalContextMenu` 렌더.
- 항목: 복사(선택 있을 때만 활성, term.getSelection()→clipboard), 붙여넣기(clipboard.readText()→sendToSession), 전체 선택(term.selectAll()), 지우기(term.clear()). 실행 후 메뉴 닫고 터미널 focus.
- 닫힘: **window 레벨 mousedown 리스너**로 메뉴 ref 바깥 클릭 감지 + ESC(TabContextMenu와 동일 패턴). 처음엔 오버레이 div로 했으나 AI 패널/탭바가 더 높은 stacking context면 오버레이가 못 덮어 안 닫히는 버그가 있어 window 리스너로 교체. 메뉴 띄운 우클릭이 즉시 닫지 않게 setTimeout(0) 후 바인딩.
- 11개 언어 라벨 전용 `CTX_STR`(STR 11블록 안 건드림).
- 메모: 마우스 모드(TUI) 중 우클릭은 앱에도 리포트가 갈 수 있음(엣지) — 필요 시 후속.

## #44 — 호스트 목록 드래그 선택 방지 + 컨텍스트 메뉴 터미널 클릭 시 닫힘

### 무엇을 / 왜 / 어떻게
- 호스트 목록 드래그 텍스트 선택: HostList 루트 div에 `userSelect: "none"`(검색 input은 editable이라 영향 없음).
- 컨텍스트 메뉴가 터미널 클릭 시 안 닫힘: xterm이 mousedown을 가로채 window 버블 리스너에 안 닿음. TabContextMenu/TerminalContextMenu의 바깥 클릭·ESC 리스너를 **캡처 단계(addEventListener(..., true))**로 변경 → xterm보다 먼저 잡아 닫힘.

## #45 — AI 패널 와이어프레임 ID(S-048)·'후속' 문구 정리

### 무엇을 / 왜 / 어떻게
- 배포 제품에 와이어프레임 ID(S-048)와 '컨텍스트 자동 첨부는 후속'(미구현) 안내가 남아 어울리지 않음.
- AIPanel.tsx 11개 언어: autoContextLater를 '활성 패널 출력을 컨텍스트로 함께 보낼 수 있어요.'류 긍정·정확 안내로 교체. includeContext 라벨에서 '(S-048)' 제거. 코드 주석 포함 S-048 전부 제거(총 12곳).

## #46 — LLM 설정: Ollama 선택 시 로컬 모델 드롭다운 표시

### 무엇을 / 왜 / 어떻게
- 백엔드: `ai_list_models(api_base, api_key?)` 커맨드 추가(ai/commands.rs) + lib.rs 등록. OpenAI 호환 `{base}/models`(Ollama의 /v1/models)로 GET, `data[].id` 파싱·정렬 반환. 키 있으면 Bearer.
- 프론트(LlmSetupModal): `isOllama = /11434|ollama/`면 apiBase 변경 시 자동으로 ai_list_models 호출. 모델 필드를 Ollama+목록 있으면 `<select>`(현재 값이 목록에 없으면 옵션으로 추가), 아니면 입력+datalist. 옆에 새로고침(↻) 버튼(모든 백엔드에서 수동 조회 가능), 실패 시 에러 표시.
- 11개 언어 refreshModels 라벨(순서 기반 스크립트 삽입).
- 참고: maxai는 사용자가 붙인 백엔드 표시 이름(빈 상태 문구가 current.displayName 사용) — 버그 아님.
- 주의: Rust 커맨드 추가라 dev 재빌드 필요.

## #47 — AI 패널 헤더 가독성(모델 드롭다운·아이콘 키움)

### 무엇을 / 왜 / 어떻게
- AI 헤더 모델 select fontSize 11→13, padding 4px6px, maxWidth 140→200. iconBtnStyle fontSize 14→17, padding 2px6px, lineHeight 1.
- 참고(사용자 질문): '활성 패널 출력을 컨텍스트로 포함'은 전송 시 포커스된 터미널의 최근 60줄을 system 메시지로 첨부(getRecentText(60))해 AI가 현재 화면을 보고 답하게 하는 토글.
